# confidence-weighted-weak-supervision
This repository explores Weakly Supervised NLP by training models on 10,000 completely unlabeled IMDB reviews. Instead of relying on expensive human annotations, I used heuristic keyword rules to generate "pseudo-labels" with assigned confidence scores (1.0 for strong matches like "masterpiece", 0.5 for weak matches like "decent").
